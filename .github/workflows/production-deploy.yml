name: Production Deploy

on: 
  push:
    branches:
      - main

jobs: 

  build:

    runs-on: ubuntu-latest
    stpes:
      - name: Checkout Source
        uses: actions/checkout@v3
      - name: Login To Docker Hub
        uses: docker login -u ${{ secrets.DOCKER_USERNAME }} - p ${{ secrets.DOCKER_PASSWORD }}
      - name: Build Docker Image
        run: docker build -t study/react-aws-app .
      - name: Publish Image To Socker Hub
        run: docker push study/react-aws-app:latest

  deploy: 
    
    needs: build
    runs-on: aws-ec2
    steps: 
      - name: Pull Image From Docker Hub
        run: docker pull study/react-aws-app:latest
      - name: Delete Old Container
        run: docker rm -f studyAwsReactContainer
      - name: Run Docker Container
        run: docker run -d -p 3000:80 --studyAwsReactContainer study/react-aws-app

